# ⚠️ 警告：此檔案含有刻意嵌入的假機密，用於展示 GHAzDO Secret Scanning 功能。
# 以下 token 為虛假值，不具任何實際效力，僅用於觸發 Secret Scanning 偵測。

app:
  name: ghazdo-vuln-demo
  version: "0.1.0"
  log_level: info
  environment: production

# Secret #2 — npm Publish Token
# Format: npm_ prefix + 36 alphanumeric characters matching 'npm_access_token'
# DEMO WORST PRACTICE: npm publish token committed to config file for CI/CD pipeline.
npm_token: "npm_K9mP2xR8vN5qT7wL3yH6jF4cA1bD0eG2sU8iO5nR3"

integrations:
  azure_devops_org: "https://dev.azure.com/my-org"
  repo_name: "azure-devops-security-worst-pratices"

# Secret #5 — Slack Incoming Webhook URL
# Format: https://hooks.slack.com/services/T.../B.../... matching 'slack_incoming_webhook_url'
# DEMO WORST PRACTICE: webhook URL hard-coded in config file committed to version control.
alerting:
  slack_webhook_url: "https://hooks.slack.com/services/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jk"
  channel: "#security-alerts"
  notify_on: ["critical", "high"]
